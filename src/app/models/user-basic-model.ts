export type UserBasicModel = {
    name: string,
    phone: string,
    email ?: string,
    dob ?: string,
    address: string,
    country: string,
    region: string,
    city: string,
    pinCode: string
};
